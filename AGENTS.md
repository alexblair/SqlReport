# SqlReport — AGENTS.md

> 所有交互对话、文档、规则、代码注释必须使用简体中文，用词严谨无歧义。开发专有词汇可用英文（如 MYSQL、PORTS）。
>
> **思考过程（thinking/reasoning）同样必须使用简体中文**——所有内部思考、推理、分析一律用简体中文完成，不得使用英文。

## 开发环境约束

**所有运行、测试操作必须在项目 VENV 中执行**，禁止使用系统 Python。VENV 路径：`venv/`。

- 运行：`venv/bin/python <脚本>`
- 测试：`venv/bin/python -m pytest tests/`
- 安装依赖：`venv/bin/pip install -r requirements.txt`

`requirements.txt` 是依赖的唯一来源（Single Source of Truth），新增/删除依赖时必须同步更新该文件。


### 开发纪律（强制遵守）

1. **依赖管控**  
   - 严禁新增任何第三方依赖或自定义组件，除非经用户明确授权且论证为“绝对必要”。  
   - 所有新增依赖须在申请时说明替代方案及影响范围。

2. **复用优先，禁止分化**  
   - 任何 UI 组件、业务模块、工具函数、类型定义等，**一律优先引用已有实现**，禁止独立编写、派生继承或创建差异化副本。  
   - 当需扩展或调整功能时，**必须直接修改原有公共模块**，实现整体提升；禁止在局部“打补丁”或另起炉灶。

3. **变更同步与测试覆盖**  
   - 任何对公共组件、模块、定义或逻辑的修改（含新增、重构、修复），**必须同步更新**：  
     - 项目知识库（README / 设计文档 / 接口契约）  
     - 关联的测试用例（单元测试、集成测试、E2E）  
   - 变更提交前，须自检是否所有引用方均被回归覆盖，避免漏测。

4. **目的追溯**  
   - 上述规则旨在**杜绝模块功能的零碎化与体验不一致**，并**确保测试用例无遗漏**，维护整体系统的可靠性与可维护性。所有成员须以此为目标执行。


## 知识库保鲜约束（FR-005）

所有 `.adocs/specs/` 下的文档必须遵守以下保鲜规则：

### 1. 提交锚定
每份分卷（模块/配置/schema 等）的 front-matter 必须包含 `last_reviewed_commit` 字段，值为最后一次人工核对该文档与代码真相一致时的 git 提交哈希。

### 2. 代码改动同步
当代码发生以下变更时，**必须同步更新关联的知识库文档**：
- 函数签名变更（参数/返回值/异常）
- 新增/删除公开 API
- 数据库 schema 变更（新增表/字段/索引）
- 配置字段变更（新增/删除/语义变更）
- 运维脚本功能变更

### 3. 保鲜核对工具
使用 `flow_docs_check` 工具定期校验文档与代码的一致性：
- **P2 开发完成时**：自动调用，报告脱节模块
- **P4 交付前**：强制调用，确保全部分卷新鲜
- **随时手动调用**：`flow_docs_check()` 或 `flow_docs_check(task_id="T-001")`

### 4. 更新流程
发现文档脱节时：
1. 阅读相关源码，确认当前真实行为
2. 更新分卷文档（保持 front-matter + 六段正文结构）
3. 更新 `last_reviewed_commit` 为当前 git HEAD
4. 运行 `flow_docs_check` 确认已修复

