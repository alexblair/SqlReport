# SqlReport 外部依赖清单
# 安装方式: pip install -r requirements.txt
# 说明：
#   mysql-connector-python — MySQL 查询连接器（必需）
#   redis                — Redis 快照缓存（可选，未安装时缓存功能自动降级）
#   markdown             — Markdown → HTML 渲染（必需，报表备注等 Markdown 内容）
#   pygments             — 代码块语法高亮（必需，配合 markdown codehilite 扩展）
#   pytest               — 测试框架（开发/测试必需）
mysql-connector-python
redis
markdown
pygments
pytest
