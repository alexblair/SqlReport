# 08 — 渲染函数测试补齐

**What to build:** 为 render.py 中所有渲染函数编写字符串匹配测试。覆盖每种渲染函数的三种状态：正常数据、空数据、错误/边界数据。测试不对比完整 HTML（太脆弱），而是验证关键结构标签存在、数据类型转换正确、空/错误状态有适当占位符。

**Blocked by:** 04 — report.py 瘦身, 05 — config.py 瘦身（渲染函数迁移到 render.py 后才能针对它们写测试）

**Status:** ready-for-agent

- [ ] 列出 render.py 中所有公开渲染函数
- [ ] 为每个函数写测试：正常数据 → 验证关键结构标签存在
- [ ] 为每个函数写测试：空数据 → 验证有适当占位符/空指示
- [ ] 为每个函数写测试：边界/错误数据 → 验证不崩溃且输出合理
- [ ] 运行全量测试，确认无回归
